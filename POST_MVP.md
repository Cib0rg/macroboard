-3. Пересмотреть интеграцию с плагинами - как они впишутся в текущий протокол? - done
5. Дизайн сделать динамическим, с возможностью смены тем
6. Драйвер для Windows с именами
7. Установщик под винду
8. Скрипт-установщик под линь (udev-rules, service etc)

10. Захват для KBD не текста, а отдельных клавиш (для захвата Enter или Ctrl) - реализовано плохо и неверно
-12. Добавить поддержку текста на изображениях - говорит что DONE, надо проверить
-13. Добавить возможность задавать яркость диодов, а не только цвет - DONE
  - глобальная задача яркости
  - перенести ползунок вправо и сделать 0-100 (сейчас 0-200)
14. Управление яркостью дисплеев, если возможно. Если нет - то просто вкл/выкл подсветку.
  - управление есть, ШИМ, надо прикрутить.
15. Мы совершенно забыли про настройки энкодера!

От ИИ
### Потенциальные улучшения (не критичные)

1. **Валидация входных данных**
   - В `ProfileService.CreateProfileAsync()` нет проверки на максимум 5 профилей
   - Рекомендация: Добавить проверку и выбросить исключение

2. **Обработка ошибок в HidDeviceManager**
   - Методы `WriteAsync()` и `ReadAsync()` логируют ошибки без деталей
   - Рекомендация: Добавить специфичные исключения (DeviceNotConnectedException, WriteTimeoutException)

3. **Таймауты команд**
   - В `DeviceService` некоторые команды могут зависнуть
   - Рекомендация: Добавить глобальный таймаут (5-10 секунд)

4. **Утечка ресурсов в плагинах**
   - `ExecutablePluginInstance` может не завершиться при сбое
   - Рекомендация: Добавить мониторинг и автоматический перезапуск


Добавить в UI ретраи для подключения к беку
Предпросмотр картинок дисплеев64
Очень долгая инициализация, если не подключён USB
Хранение картинок с CRC, чтобы не дублировать ?

Не работает на винде, нет libusb-библиотек.