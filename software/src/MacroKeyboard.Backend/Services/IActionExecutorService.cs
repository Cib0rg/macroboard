namespace MacroKeyboard.Backend.Services;

/// <summary>
/// Listens to device button events and executes PC-side actions (LaunchApp, Shell, Plugin).
/// </summary>
public interface IActionExecutorService { }
